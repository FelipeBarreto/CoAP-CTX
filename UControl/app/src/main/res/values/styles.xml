<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@color/fallbackColor</item>
        <item name="android:textColorSecondary">@color/fallbackColor</item>
    </style>

</resources>

